%h2 Resend confirmation instructions
= simple_form_for(@user, :as => :user, :url => confirmation_path(:user), :html => { :method => :post }) do |f|
  %p
    = f.input :email ,
              :placeholder => "Email", 
              :label => false, 
              :class => 'inputbox', 
              :hint => "Enter the email to confirm again.",
              :input_html => { :size=> 30  }
  %p= f.submit :value=>"Resend confirmation instructions"
